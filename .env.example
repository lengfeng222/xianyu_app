# APP 后端 API 地址（必须含 /api/v1，与后端 API_V1_PREFIX 一致）
# 部署到服务器时改为实际后端地址，例如：
# 使用 APP 专用 HTTPS 域名。改好后复制为 .env 并重新打包 APP。
# Test branch default. Production APKs must be built from the production branch/worktree.
EXPO_PUBLIC_API_BASE_URL=http://101.37.195.56:8000/api/v1
